const numerito = 25;
const numeritoDecimal = 100.5;
const saludo = "hola";
const check = true;

console.log(`Numero: ${Number(numerito)} | Numero decimal: ${Number(numeritoDecimal)} | Saludo: ${Number(saludo)} | Check: ${Number(check)}`)